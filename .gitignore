# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets (public repo — 混入は致命的)
.env
.env.*
!.env.example
*.pem
*.key
*_secret*
*_credentials*

# 検証用・一時アセット (DR-2026-002: 専用ディレクトリに限定し rm で個別削除)
/.tmp/
public/tmp/
*.tmp.glb
*.tmp.png

# ローカルツール状態 (プロダクト外)
.agents/
.claude/skills/
skills-lock.json

# 注: public/draco/ 等の正規アセットは意図的にコミットする。
# GLB/画像/録画などユーザーデータは public/tmp/ か作業ディレクトリ外に置き、本リポジトリにコミットしない。
