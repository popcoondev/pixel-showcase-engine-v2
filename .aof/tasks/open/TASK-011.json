{
  "task_format_version": 1,
  "id": "TASK-011",
  "title": "Firebase Hosting デプロイ + CI",
  "status": "open",
  "phase": "phase1-slice",
  "priority_rank": 1,
  "owner_role": "Builder",
  "estimate": "S-M",
  "rationale": "Phase1 の主作業。Vite 本番ビルドを Firebase Hosting に載せ、公開URLでエディタがブラウザで動く状態にする。共有は既存の PNG/mp4 書き出しで成立。",
  "implementation_hint": "firebase.json (public=dist, SPA rewrite=/index.html), vite base はルート配信なら '/' のまま可。GitHub Actions: main push で npm ci → npm run build → firebase deploy (FIREBASE_TOKEN or workload identity)。DRACO の public/draco/ がデプロイに含まれることを確認。",
  "prerequisites": ["human が Firebase プロジェクト作成 + project id 共有", "デプロイ用の認証 (CI secret)"],
  "dependencies": ["TASK-002"],
  "acceptance": "公開URLでエディタが動作し、GLB(Draco含む)配置・Preview・PNG/mp4 書き出しができる。main push で CI が build green を確認しデプロイ。"
}
